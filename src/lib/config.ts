/**
 * Значения, которые не правятся из админки.
 *
 * Всё, что заказчик меняет сам — города, цена голоса, тексты, открыт ли раздел —
 * живёт в базе (см. content.ts) и правится в /admin.
 */

/**
 * Разрешённые пакеты голосов. Ничего кроме них принять нельзя — ни с сайта,
 * ни запросом напрямую.
 */
export const VOTE_PACKS = [1, 5, 10];

/**
 * Пакеты, доступные до первого голоса.
 *
 * Сначала человек проходит весь путь с одним голосом и убеждается, что он
 * работает; крупные пакеты открываются только после этого.
 */
export const FIRST_VOTE_PACKS = [1];

export const MAX_VOTE_AMOUNT = 1_000_000;

/** Максимум цветовых схем карточек в оформлении. */
export const MAX_COLOR_INDEX = 3;

/**
 * Ограничение на запись заявок.
 *
 * Оплата автоматически не проверяется, поэтому это единственный барьер против
 * того, чтобы счётчик накрутили скриптом.
 */
export const RATE_LIMIT = {
  windowMinutes: 10,
  maxPerIp: 40,
};

export const PROJECT_STATS = [
  { value: 20, label: 'өңір' },
  { value: 60, label: 'ақын' },
  { value: 20, label: 'команда' },
];
